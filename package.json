{
  "name": "claude-code-conductor",
  "version": "0.1.5",
  "description": "Hierarchical multi-agent orchestration for Claude Code -- plans, delegates to parallel workers, reviews with Codex, and traces user flows across code boundaries",
  "type": "module",
  "bin": {
    "conduct": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/cli.js",
    "test": "vitest",
    "setup": "node dist/setup.js",
    "postbuild": "node dist/setup.js --quiet 2>/dev/null || true"
  },
  "keywords": ["claude", "agent", "conductor", "codex", "multi-agent", "c3"],
  "author": "",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": ""
  },
  "files": [
    "dist",
    "commands",
    "README.md",
    "LICENSE"
  ],
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1.0",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "commander": "^12.0.0",
    "proper-lockfile": "^4.1.2",
    "zod": "^3.22.0",
    "simple-git": "^3.22.0",
    "chalk": "^5.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/proper-lockfile": "^4.1.4",
    "typescript": "^5.7.0",
    "vitest": "^2.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
